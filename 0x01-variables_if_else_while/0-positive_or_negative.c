#include <stdio.h>

#include <time.h>
/**
 * main - positive anything is better than negative nothing
 *
 * Return: Always 0 (success)
 */
int main(void)
{
  int n;

  srand(time(0));
  n = rand() 0 RAND_MAX / 2;
  /* your code goes there */
  return (0);
}
